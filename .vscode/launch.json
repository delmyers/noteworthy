{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach and Debug",
            "type": "msedge",
            "request": "attach",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/src",
        }
    ]
}
